# 캠퍼스 라이프 RPG (Campus Life RPG) 개발 현황 및 로드맵

이 문서는 프로젝트의 현재 진행 상황을 파악하고, 앞으로 프론트엔드, 백엔드, 데이터베이스(DB)에서 구현해야 할 세부 기능과 로드맵을 정리한 문서입니다.

---

## 1. 현재까지 진행된 사항

### 1.1. 데이터베이스 (완료)

- `database/schema.sql`: `users`, `pets`, `stats`, `schedules`, `quests`, `user_quests`, `friends`, `items` 테이블 정의.
- `database/seed.sql`: 테스트용 더미 데이터.
- **참고:** 로그인을 카카오 등 소셜로 전환할 경우 `users`의 `email`/`password` 필드는 `kakao_id` + nullable password 등으로 마이그레이션할 수 있음.

### 1.2. 프론트엔드 UI 및 로컬 상태

**공통**

- **Layout:** 좌우 장식 패널 + 중앙 폰 프레임(로그인·회원가입 시에만 기기 플로팅 애니메이션).
- **TopBar:** 투명 바, 프로필·닉네임·**픽셀 코인 이미지**, 상점/설정, 홈과 동일한 HUD 스타일(반투명 버튼·밝은 텍스트 대비).
- **BottomNav:** 스탯·일정·홈·퀘스트·친구, 픽셀 아이콘·탭 구분선·단색+디더 타일 배경·선택 탭 하이라이트.
- **폰트:** `:root`에서 `--body-font` = `--pixel-font` (DotGothic16 통일).

**페이지**

- **로그인 (`/login`):** 이메일/비밀번호 폼(임시), 모험 시작 → `/home` (추후 **카카오 로그인**으로 교체 예정).
- **홈 (`/home`):** `home-immersive`로 숲 배경이 **TopBar까지** 이어짐, 레벨/EXP/알/정보 박스.
- **투두 (`/todo`):** 주간 캘린더, 일정 CRUD(로컬 state).
- **퀘스트 (`/quests`):** 일일/주간, **로컬 `server/quest-api.mjs` + Gemini** 맞춤 생성(Vite 프록시), 프로필 학과·학년 연동.
- **스탯 (`/stats`):** Chart.js 레이더, 5스탯·피로도 UI.
- **친구 (`/friends`):** 목록·검색·`/profile/friend/:id` 프로필(목 데이터).
- **프로필 (`/profile`):** OS 창 스타일, 펫·필드 편집, `ProfileContext` + localStorage.
- **상점 (`/shop`):** NPC 헤더, **코인 이미지**, 아이템 리스트.
- **설정:** 모달(볼륨·로그아웃 등, localStorage).

**백엔드 관련(이미 존재)**

- `**backend/`:** Express + `mysql2` 연결 뼈대(`server.js`, `db.js`), 포트 기본 5555(macOS AirPlay가 5000 점유 시 대비) — **게임 API는 여기서 로컬 구축 예정**.
- `**server/quest-api.mjs`:** Express, `POST /api/quests/generate` (Gemini), 프론트는 Vite가 `8787`으로 프록시.

### 1.3. 보류·의도적

- **회원가입 전용 페이지:** 소셜 로그인 정책 확정 후 (카카오 최초 로그인 시 자동 가입 등).
- 일부 화면은 여전히 **목 데이터 / localStorage** 기준.

---

## 2. 백엔드 연결 방향 (요약)


| 구분          | 방식                                                                                                  |
| ----------- | --------------------------------------------------------------------------------------------------- |
| **로그인**     | **카카오(또는 유사) OAuth** — 인가 코드·액세스 토큰은 카카오 API, **우리 서버**에서 사용자 식별 후 **JWT(또는 세션) 발급** 및 `users` 동기화. |
| **그 외 API** | **로컬 백엔드** (`backend/` + MySQL) — 프로필, 코인, 펫, 일정, 퀘스트(마스터), 친구, 상점 등.                               |
| **LLM 퀘스트** | 현재처럼 `**server/quest-api.mjs`** 유지 또는 나중에 `backend`에 라우트 통합(선택).                                    |


프론트 개발 시: 로그인만 카카오 리다이렉트/팝업 플로우, 이후 요청은 `Authorization: Bearer <JWT>`로 **동일한 로컬 API 베이스 URL**을 사용하면 됩니다.

---

## 3. 백엔드 진행 순서 (권장)

아래 순서대로 진행하면 의존 관계가 단순합니다.

1. **로컬 API 뼈대 확정**
  - `backend/server.js`에 `GET /api/health`, `GET /api/items` 등 **인증 없이** DB 조회 1~2개 연결해 CORS·`.env`·MySQL 풀 검증.
2. **사용자 모델과 카카오 연동 설계**
  - 카카오에서 받을 식별자(예: 고유 ID) 저장 컬럼 추가·마이그레이션.  
  - 플로우: 프론트 → 카카오 로그인 → **리다이렉트 URI는 백엔드** 또는 프론트에서 코드 수신 후 백엔드에 전달 → 백엔드가 카카오 토큰 API 호출 → `users` upsert → **JWT 발급** → 프론트에 쿠키 또는 JSON으로 전달.
3. **인증 미들웨어**
  - 보호 라우트: `Authorization` 헤더 검증 후 `req.userId` 주입.
4. **도메인 API 순차 연동**
  - 프로필·코인·EXP → 펫·스탯 → 일정 → 퀘스트(서버 할당/완료) → 친구 → 상점 구매.
5. **프론트 치환**
  - `axios`/fetch 베이스 URL, 토큰 저장소, 보호 라우트, 화면별 목 데이터 제거.
6. **(선택)** `quest-api.mjs`를 `backend`에 합치거나, 게이트웨이 한 곳에서 포트만 노출.

---

## 4. 향후 개발 로드맵 (To-Do)

### 4.1. 데이터베이스

- 소셜 로그인용 컬럼(`kakao_id` 등), `password` nullable 등 스키마 조정.
- (선택) 인벤토리·구매 이력, 펫 마스터.
- 운영용 마이그레이션 전략 (`ALTER` 위주).

### 4.2. 백엔드 (`backend/`)

- CORS, JSON, `.env`, `mysql2` 풀, 라우터 분리.
- `**/api/auth/kakao` (또는 콜백)** + JWT 발급·검증 미들웨어.
- 유저·프로필, 펫·스탯, 일정, 퀘스트, 친구, 상점 API (스키마와 1:1 매핑).

### 4.3. 프론트엔드

- 카카오 SDK 또는 리다이렉트 로그인 버튼, 콜백 페이지(필요 시).
- API 클라이언트 + 토큰 저장 + 보호 라우트.
- TopBar/홈/각 탭: 서버 데이터 기준으로 갱신.

---

## 5. 지금 당장 하면 좋은 다음 스텝

1. `**backend`에서 DB 붙은 읽기 API 1개** 동작 확인.
2. **카카오 개발자 콘솔**에 앱 등록, Redirect URI를 로컬 백엔드(또는 프론트)와 맞춤.
3. `**POST /api/auth/kakao`** (코드 또는 액세스 토큰 받아서 유저 생성/조회 + JWT) 최소 구현.
4. 로그인 페이지에서 **이메일 폼 대신 카카오 버튼** 연결(개발 단계에서는 둘 다 유지 가능).

---

## 6. 참고 파일


| 구분          | 경로                                         |
| ----------- | ------------------------------------------ |
| DB 스키마      | `database/schema.sql`                      |
| DB 시드       | `database/seed.sql`                        |
| 메인 API (로컬) | `backend/server.js`, `backend/db.js`       |
| LLM 퀘스트 API | `server/quest-api.mjs`, `server/README.md` |
| 프론트 프록시     | `frontend/vite.config.js` (`/api` → 8787)  |


---

*마지막 업데이트: TopBar/BottomNav·홈 immersive·코인 이미지·폰트 통일·카카오 로그인 + 로컬 API 전략 반영.*